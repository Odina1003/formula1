from championship import Championship

class Driver:
    def __init__(self, name:Championship) -> None:
        self.name: list(Championship) = []

    def getname(self, name):
        for i in self.name:
            return name
        